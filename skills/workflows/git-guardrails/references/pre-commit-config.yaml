# .pre-commit-config.yaml — framework-based hooks (https://pre-commit.com)
# Install: pip install pre-commit && pre-commit install
# Run all: pre-commit run --all-files
# Update hooks: pre-commit autoupdate

repos:
  # General file hygiene
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        args: [--allow-multiple-documents]
      - id: check-json
      - id: check-added-large-files
        args: [--maxkb=5000]
      - id: check-merge-conflict
      - id: detect-private-key
      - id: no-commit-to-branch
        args: [--branch, main, --branch, production, --branch, homologation]

  # Secrets detection
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks

  # Conventional Commits
  - repo: https://github.com/compilerla/conventional-pre-commit
    rev: v3.4.0
    hooks:
      - id: conventional-pre-commit
        stages: [commit-msg]

  # Python (uncomment if applicable)
  # - repo: https://github.com/astral-sh/ruff-pre-commit
  #   rev: v0.5.0
  #   hooks:
  #     - id: ruff
  #       args: [--fix]
  #     - id: ruff-format

  # Terraform (uncomment if applicable)
  # - repo: https://github.com/antonbabenko/pre-commit-terraform
  #   rev: v1.92.0
  #   hooks:
  #     - id: terraform_fmt
  #     - id: terraform_validate
  #     - id: terraform_tflint

  # Helm (uncomment if applicable)
  # - repo: https://github.com/gruntwork-io/pre-commit
  #   rev: v0.1.23
  #   hooks:
  #     - id: helmlint

  # Markdown lint (uncomment if applicable)
  # - repo: https://github.com/igorshubovych/markdownlint-cli
  #   rev: v0.41.0
  #   hooks:
  #     - id: markdownlint
  #       args: [--fix]
